server:
  port: 0

spring:
  application:
    name: account-management-service
  cloud:
    consul:
      host: consul
      port: 8500
      discovery:
        instance-id: ${spring.application.name}-${random.int}